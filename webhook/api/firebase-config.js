// Firebase Configuration for your project
// Replace these with your actual Firebase project details

const firebaseConfig = {
  // You can find these in your Firebase Console → Project Settings → General
  projectId: "salesiq-f0530", // Replace with your actual project ID
  
  // For local development, you can also use:
  // - Service account JSON file (recommended for production)
  // - Firebase emulator (for testing)
  // - Application Default Credentials (if running on Google Cloud)
};

module.exports = firebaseConfig;
